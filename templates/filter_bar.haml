-load events_tags

#filter_bar.snap-drawers.row
  .snap-drawer.snap-drawer-left.twelve.columns
    %h2
      Selection
    %hr
    %h3
      By sources
    %ul#sources
      -for source, options in sources
        %li{style: "margin: 3px"}
          %span{data: 'background-color: {{ options.bg|default:"black" }}; color: {{ options.fg|default:"white" }}; padding: 3px; padding-left: 6px; padding-right: 6px;'}= source|upper|underscore_to_space
    %h3
      By tags
    %ul#tags
      -for name in tags
        %li
          %span= name
